import { CarsfilterPipe } from './carsfilter.pipe';

describe('CarsfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CarsfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
